

            <div class="container-fluid px-10">
              <div class="container mt-5 mx-auto col-10 col-md-8 col-lg-6">
                <h3 class="fs-10 mb-3">Edit User</h3>
                <div class="col">
                  <form method="post" action="/edit_user">
                    <div class="row">
                      <div class="col-md-6">
                        <input type="number" name="id" value="<%= data[0].user_id %>" hidden>
                        <div class="form-group">
                          <label for="username">First Name:</label>
                          <input type="text" id="username" name="username" value="<%= data[0].first_name%>" class="form-control">
                        </div><br />
                      </div>
                      <div class="col-md-6">
                        <div class="form-group">
                          <label for="lastname">Last Name:</label>
                          <input type="text" id="lastname" name="lastname"  value="<%= data[0].last_name%>" class="form-control">
                        </div>
                      </div><br />
                    </div>
                    <div class="form-group">
                      <label for="phonenumber">Phone Number:</label>
                      <input type="text" id="phonenumber" name="phonenumber"  value="<%= data[0].contact_no%>" class="form-control">
                    </div><br /> 
                    <div class="row">
                      <div class="col-md-12">
                        <div class="d-flex justify-content-around">
                          <button type="submit" class="btn btn-success">Submit</button>
                          <a href="/users/viewall_users" class="btn btn-success" style="text-decoration: none; color: white;">Back</a>
                        </div>
                      </div>
                    </div>
                  </form>
                </div>
              </div>
            </div>
        </div>
    </div>
    <!-- /#page-content-wrapper -->
    </div>

    <!-- Bootstrap CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script> -->

    <script>
    var el = document.getElementById("wrapper");
    var toggleButton = document.getElementById("menu-toggle");
    
    toggleButton.onclick = function () {
        el.classList.toggle("toggled");
    }; 
    
    function previewImage(event) {
        var reader = new FileReader();
        reader.onload = function() {
          var output = document.getElementById('preview');
          output.src = reader.result;
        };
        reader.readAsDataURL(event.target.files[0]);
    };

    function filterCurrent() {
      var input, filter, ul, li, a, i, match;
      input = document.getElementById("currentLocation-search");
      if(input.value === "") {return;}
      filter = input.value.toUpperCase();
      div = document.getElementById("currentOptions");
      a_label = div.getElementsByTagName("label");
      a_radio = div.getElementsByTagName("input");
      match = 0;
      for (i = 0; i < a_label.length; i++) {
        if (match > 5){ 
          for (i = 0; i < a_label.length; i++) {
            if(a_radio[i].ischecked){
              a_radio[i].style.display = "inline";
              a_label[i].style.display = "inline";
            }
            else{
              a_radio[i].style.display = "none";
              a_label[i].style.display = "none";
            }
          }
          break; 
        }
        txtValue = a_label[i].textContent || a_label[i].innerText;
        if (txtValue.toUpperCase().indexOf(filter) > -1) {
          a_radio[i].style.display = "inline";
          a_label[i].style.display = "inline";
          match += 1;
        } else {
          if(a_radio[i].checked){
            a_radio[i].style.display = "inline";
            a_label[i].style.display = "inline";
          }
          else{
            a_radio[i].style.display = "none";
            a_label[i].style.display = "none";
          }
        }
      }
    } 

    function filterPrevious() {
      var input, filter, ul, li, a, i, match;
      input = document.getElementById("previousLocation-search");
      if(input.value === "") {return;}
      filter = input.value.toUpperCase();
      div = document.getElementById("previousOptions");
      a_label = div.getElementsByTagName("label");
      a_radio = div.getElementsByTagName("input");
      match = 0;
      for (i = 0; i < a_label.length; i++) {
        if (match > 5){ 
          for (i = 0; i < a_label.length; i++) {
            if(a_radio[i].ischecked){
              a_radio[i].style.display = "inline";
              a_label[i].style.display = "inline";
            }
            else{
              a_radio[i].style.display = "none";
              a_label[i].style.display = "none";
            }
          }
          break; 
        }
        txtValue = a_label[i].textContent || a_label[i].innerText;
        if (txtValue.toUpperCase().indexOf(filter) > -1) {
          a_radio[i].style.display = "inline";
          a_label[i].style.display = "inline";
          match += 1;
        } else {
          if(a_radio[i].checked){
            a_radio[i].style.display = "inline";
            a_label[i].style.display = "inline";
          }
          else{
            a_radio[i].style.display = "none";
            a_label[i].style.display = "none";
          }
        }
      }
    } 
    </script>
</body>

</html>